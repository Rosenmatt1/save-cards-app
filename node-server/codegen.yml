overwrite: true
schema: "schema.graphql"
documents: null
generates:
  graphql-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./schema.json:
    plugins:
      - "introspection"
